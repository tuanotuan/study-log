services:
  - type: web
    name: logstudy
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm run db:ensure && npm run start
    autoDeployTrigger: commit
    envVars:
      - key: NODE_VERSION
        value: 22.12.0
      - key: DATABASE_URL
        value: file:/tmp/logstudy.db
      - key: UPLOAD_DIR
        value: /tmp/logstudy-uploads
      - key: APP_URL
        value: https://logstudy.onrender.com
      - key: SESSION_SECRET
        generateValue: true
